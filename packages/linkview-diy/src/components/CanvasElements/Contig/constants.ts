export const MAX_CONTIG_LENGTH = 500000000;
export const DEFAULT_THICKNESS = 36;

export const CAPTURE_CONTROLLER_THICKNESS = 5;
export const CAPTURE_CONTROLLER_WRAPPED_LEN = 5;

export enum TickMode {
  ShowVisibleInterval = 0,
  ShowWhole,
  NotShow,
}

export enum TickPos {
  top,
  bottom,
}
